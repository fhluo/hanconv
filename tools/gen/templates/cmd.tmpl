package cmd

import (
	"github.com/fhluo/gocc/pkg/cc/{{.packageName}}"
	"github.com/spf13/cobra"
)

var {{.name}}Cmd = &cobra.Command{
	Use:   "{{.use}}",
	Short: "{{.short}}",
	RunE: func(cmd *cobra.Command, args []string) error {
		return run(cmd, {{.packageName}}.Convert)
	},
}

func init() {
	{{.name}}Cmd.Flags().StringVarP(&inputFilename, "input", "i", "", "input filename")
    {{.name}}Cmd.Flags().StringVarP(&inputFilename, "input", "i", "", "output filename")

    Commands = append(Commands, {{.name}}Cmd)
}
